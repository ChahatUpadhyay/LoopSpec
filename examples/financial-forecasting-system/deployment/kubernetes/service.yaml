apiVersion: v1
kind: Service
metadata:
  name: financial-forecasting-service
  labels:
    app: financial-forecasting
    component: api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: financial-forecasting
    component: api
